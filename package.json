{
  "name": "@modern-mean/mongoose-module",
  "version": "0.0.0",
  "description": "Modern-Mean module for MongoDB via Mongoose",
  "keywords": "modern-mean,mongodb,mongo,mongoose,database",
  "main": "/dist/index.js",
  "engines": {
    "node": ">=6.4",
    "npm": ">=3.10.3"
  },
  "scripts": {
    "coverage": "nyc report --reporter=text-lcov | coveralls",
    "build": "rm -rf ./dist && babel --out-dir ./dist --copy-files ./src",
    "build:watch": "rm -rf ./dist && babel --out-dir ./dist --copy-files --watch ./src",
    "lint": "eslint ./tests/** ./src/**",
    "postversion": "git push && git push --tags && npm publish --access public",
    "release:patch": "npm test && npm run build && npm version patch",
    "release:minor": "npm test && npm run build && npm version minor",
    "release:major": "npm test && npm run build && npm version major",
    "test": "npm run lint && cross-env NODE_ENV=test nyc --reporter=lcov --reporter=text mocha --compilers js:babel-core/register --require tests/mocha.setup.js tests/*.spec.js",
    "test:watch": "npm run lint && cross-env NODE_ENV=test nyc --reporter=lcov --reporter=text mocha --watch --compilers js:babel-core/register --require tests/mocha.setup.js tests/**/*.spec.js"
  },
  "author": "Andrew Throener",
  "dependencies": {},
  "nyc": {
    "sourceMap": false,
    "instrument": false
  },
  "license": "Apache-2.0",
  "repository": {
    "type": "git",
    "url": "https://github.com/modern-mean/mongoose-module.git"
  },
  "bugs": {
    "url": "https://github.com/modern-mean/mongoose-module/issues"
  },
  "homepage": "https://github.com/modern-mean/mongoose-module#readme"
}
